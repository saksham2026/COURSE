#include<iostream>
using namespace std;
int arr[10]; // All the indexes will be having the value to be 0.
string s ; // This will also be a null string, "\0".
char ch ;  // This will store a null character. '\0' is called the null character.
int main() {

    /*
    string s1 ;
    if( s == "\0") {
        cout << "Yes, it is a null character" << endl;
    }
    cout << s1 << endl;
    */

   /*
   int arr[] = {1,2,3};
   cout << sizeof(arr)/sizeof(arr[0]);

   arrays are derived data types.
   */

  // When we create pointers, the pointers are stored in stack memory.


    return 0;
}